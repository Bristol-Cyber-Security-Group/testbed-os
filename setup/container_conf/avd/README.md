TODO - AVD should have its own service.
This service should try to fix the problems we encounter around the display mode in clustering.
So it manages or has a display variable to give to the emulators.
Then it should also hold the pid of the emulators that are running, to make it easier to manage emulator instances and not orphan them.


